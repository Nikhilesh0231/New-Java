package Security;

import javax.swing.*;

public class DecryptionForm extends JFrame {
    public DecryptionForm() {
        setTitle("Decryption");
        setSize(400, 300);
        setLocationRelativeTo(null);
        add(new JLabel("Decryption UI goes here...", SwingConstants.CENTER));
    }
}

