public class TernaryOperator {
  public static void main(String[] args) {
    boolean isAdult = true;
    String result = (isAdult == true) ? "You can vote" : "You can't vote";
    System.out.println(result);
  }
}
